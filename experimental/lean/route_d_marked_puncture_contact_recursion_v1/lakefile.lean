import Lake
open Lake DSL

package «route_d_marked_puncture_contact_recursion_v1» where
  -- Standalone stdlib-only theorem interface and exact finite pins.

@[default_target]
lean_lib «RouteDMarkedPunctureContactRecursionV1» where
  roots := #[`RouteDMarkedPunctureContactRecursionV1]
