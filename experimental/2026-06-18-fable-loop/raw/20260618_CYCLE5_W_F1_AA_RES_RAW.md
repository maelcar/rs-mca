I'll start by reading the file index, then work through the required files in order.Let me read the core required files in parallel.Now the two required audits and the key state files.Let me read the remaining state files and the Cycle 1/3 audits, plus the scratch.Now let me read the key source definitions. Let me grep for the labels in slackMCA_v3.tex.Now let me read the source definitions in slackMCA_v3.tex. Let me find the relevant labels first.Let me read the `prob:F1` region of the blueprint and the rigidity statements.This reframing is crucial: the slope condition `interp_S(w) ≡ z·Bnum (mod E)` with `deg interp_S(w) < a = k+t` means `interp_S(w) - z·Bnum = E·P_z` with `deg P_z < k`, i.e. `z` is bad exactly when `(w - z·Bnum)/E` agrees with a degree-`<k` polynomial on `a` points. That's a line-decoding/MCA statement. Let me check the banked counterpackets to see whether the balanced case collapses to one already cut. Reading the most relevant audits.I have enough source-valid material to classify. Let me verify one more time the rigidity/no-anchor boundary the bound would have to cross, since it determines whether a packing bound is even reachable from source tools.Final classification: EXACT_NEW_WALL

Object:
`W-F1-AA-RES` — the reserve-indexed paired-readout slope-image scale. The first missing invariant is a characteristic-zero rigidity/value-count law for the paired interpolation-residue readout `rho` modulo `Ehat` (the arbitrary-anchor analogue of `thm:rigidcyclo`/`thm:exactcount`), together with the reserve threshold `eta=sigma/n` and the generated-field ledger `prob:F2` that decide whether the mod-`p` image of that count is `Theta(q_line)` or `n^{1+o(1)}`. "Balanced `t=sigma`" alone is not a wall; it is a one-parameter family in `eta=sigma/n`.

Short verdict:
The restored `W-F1-AA` count is exactly `Lambda^{NC}_{t,delta}` of `def:residue` for the datum `(E,Bnum,w)` in the balanced case, so it is a faithful arbitrary-anchor extension-denominator instance of the open core `prob:perfiber`, not a smaller object. The wall names two separations (tangent/zero-numerator and quotient-periodic) and asks for a bound, but those two separations do not bind: the banked F1 Fixed-Rate Sigma-1 Counterpacket survives both and already gives `Theta(q_line)` distinct balanced slopes. The quantity that actually controls the count is the reserve `eta=sigma/n`, which the wall omits. So the right output is not a packing proof (that is `conj:B`/`prob:perfiber`, open) nor a fresh counterpacket (the strong one is banked, at sub-reserve) nor a route cut (the wall is a valid `def:residue` specialization). It is the isolation of the missing invariant: the reserve-thresholded, quotient-profile-corrected slope-image scale and the rigidity law for `rho` that is logically prior to it.

Proof / counterpacket / wall:

Step 1 — the wall count is exactly `Lambda^{NC}` for the datum (no smaller object). Fix a balanced datum `(E,Bnum,w)`, `deg E=t=sigma`, `E` nonzero on `D`, `deg Bnum<t`, `Bnum!=0`, `a=k+t=s_delta`. A scalar `z in F` is counted iff some `a`-subset `S` has `[interp_S(w)]_E = z[Bnum]_E`. Put `Q_z:=interp_S(w)`, `deg Q_z<a=k+t`. Then `Q_z ≡ z·Bnum (mod E)`, `Q_z=w` on `S`, and `|S|=a=s_delta`. This is verbatim a witness `(Q_z,S_z=S)` of `def:residue`. Noncontainment holds automatically: by the banked Cycle 3 / Codex noncontainment subset lemma, `Bnum!=0` and `|S|=a=k+t` force `E·G+Bnum` to have degree `<a` while vanishing on `a` points, so no `G in F_{<k}` satisfies `G=-Bnum/E` on `S`. By the Cycle 2 caveat, `z` is the unique scalar whenever `[Bnum]_E!=0` (nonzero field scalars are units mod `E`, even when `[Bnum]_E` is a zero divisor). Conversely a noncontained witnessed slope of the datum yields such an `S` by shrinking `S_z` to an `a`-subset (Cycle 1 forced interpolant survives, Cycle 3 noncontainment survives). Hence

```text
#{slopes in W-F1-AA for this datum} = #{noncontained-witnessed slopes of (E,Bnum,w)} ≤ Lambda^{NC}_{t,delta}(D,k),
```

with equality attained at the extremal datum. The Cycle 1–2 paired readout `rho(S)=(interp_S(w0) mod Ehat, interp_S(w1) mod Ehat)`, `Ehat=lcm(E,E^tau) in B[X]`, is a faithful base-field repackaging of `[interp_S(w)]_E`; it changes the coordinates, not the count. So `W-F1-AA` is the arbitrary-anchor, extension-denominator (`E in F[X]\B[X]`) instance of `prob:perfiber`'s packing number, at radius with `s_delta=k+sigma`. The monic locator/prefix map `Phi_sigma` of `prob:perfiber` is the special case `w=x^T`, `rho(S)=[L_S]_Ehat`.

Step 2 — the radius is `eta=sigma/n`. From `s_delta=ceil((1-delta)n)=k+sigma` one gets `delta=1-rho-sigma/n`, i.e. reserve `eta=sigma/n`. The source positive prediction (`conj:B`, `rem:aper`) bounds the quotient-separated `Lambda^{aper}_{t,1-rho-eta}` by `n^{1+o(1)}` only when `eta>=(1+eps)taustar(rho,q_{D})`, equivalently (matching `prob:perfiber`) `sigma>=Cn/log_2 n`, i.e. `eta>=C/log_2 n`. For any fixed `sigma` (in particular the named test cases `sigma=1,2`), `eta=sigma/n -> 0`, far below `taustar ≍ 1/log n`. So "balanced `t=sigma` at fixed `sigma`" sits strictly in the sub-corrected-reserve regime.

Step 3 — the two named separations do not bind; the count is `Theta(q_line)` after both. Take the banked F1 Fixed-Rate Sigma-1 Counterpacket as an instance of `W-F1-AA`: `B=F_p`, `F=F_{p^2}`, `E=X-alpha` with `alpha notin B` (so `E in F[X]\B[X]`, extension denominator, `t=1=sigma`, balanced), `Bnum=1!=0` (so not zero-numerator/tangent), anchor `w=E·f=x^a` (a base-valued arbitrary-anchor instance, `w1=0`). For each `a`-subset `S`, `Q_S=X^a-L_S`, slope `z_S=Q_S(alpha)`; the map `{x,y}->z_{T∪{x,y}}` over a fixed `(a-2)`-set `T` is injective in `(x+y,xy)`, giving

```text
#distinct slopes ≥ binom(p-a+1,2)/... = Theta(p^2) = Theta(q_line).
```

Check the wall's own separations: (i) tangent/zero-numerator — `Bnum=1!=0`, excluded; each `z_S` is genuinely MCA-bad, hence noncontained by Step 1. (ii) quotient-periodic (`rem:aper`: denominator a pullback through `x->x^M`, `M|gcd(n,k)`, `M>1`) — `E=X-alpha` is degree 1, not such a pullback, so these lines are aperiodic and survive into `Lambda^{aper}`. Therefore the `Theta(q_line)` count survives both separations the wall names. The two separations remove the tangent floor (`prop:floor`/`rem:strata`) and the quotient-profile term (`thm:qnecessity`/`rem:aper`); they do not touch the reserve dependence. This is the decisive point: the wall asks for a bound after exactly the separations that provably leave the count at `Theta(q_line)`.

Step 4 — therefore the binding, omitted invariant is the reserve scale, and structurally-prior to it, a rigidity law for `rho`. Two distinct things are missing from "paired-readout slope-image packing":

(a) Reserve threshold. "Balanced" fixes `eta=sigma/n`. The slope image undergoes a phase transition: sub-reserve (fixed `sigma`, e.g. `sigma=2`) it is `Theta(q_line)` (Step 3, banked at `sigma=1`; the interrupted `p=11,sigma=2` scratch showed `best_random_slopes=(10,10)` vs `monic=(9,9)` — a real arbitrary-vs-monic gap but tiny numbers, explicitly NOT a counterpacket); above corrected reserve `sigma>=Cn/log_2 n` it is conjecturally `n^{1+o(1)}` after quotient separation (`conj:B`), which is open. The wall must be reserve-indexed: `Lambda^{aper}_{sigma,1-rho-eta}` as a function of `eta`, with threshold `eta*_{AA}(rho,q_gen)` the arbitrary-anchor extension-denominator analogue of `taustar`.

(b) Rigidity/value-count law for `rho`, logically prior. The monic packing theory works because `thm:rigidcyclo` characterizes prefix collisions of `Phi_sigma` as antipodal rearrangement, which `thm:exactcount` turns into the characteristic-zero value count `A(N',ell')`, reduced mod `p` above the quotient norm threshold (`prop:qfloor`). For the arbitrary-anchor readout `rho`, `interp_S(w_i)` is a full interpolation residue, not a symmetric-function prefix, so there is no `thm:rigidcyclo` analogue: no collision law for `rho(S)=rho(S')` on the bad line, hence no characteristic-zero value count to reduce, hence no quotient profile `Q_H(eta)` to read off. This missing rigidity law is "sharper than paired-readout slope-image packing" because it is the object a packing bound would be derived from; the reduction (Cycles 1–2) delivered the readout's domain `(B[X]/Ehat)^2` (a `q_gen`-entropy object) but not its collision law.

(c) Field ledger. The reduction is a `q_gen=p` entropy statement (readout components in `B[X]`), while slopes live in `q_line=p^2`. A useful bound needs the `prob:F2` generated-field statement that the above-threshold slope-image entropy is carried by `q_gen` and the quotient profile, not by ambient `q_line`. Step 3 shows this fails below reserve (image fills `q_line`), so the ledger claim is itself reserve-conditional — confirming (a) is the gateway invariant.

Statement of the exact new wall:

```text
W-F1-AA-RES (first missing invariant, sharper than paired-readout packing):
For quadratic B=F_p, F=F_{p^2}, D⊂B, E in F[X]\B[X] of degree t=sigma nonzero on D,
Bnum!=0, arbitrary w=w0+alpha w1, paired readout rho(S)=(interp_S(w0),interp_S(w1)) mod Ehat:
(1) Produce a rigidity/value-count law for rho on the bad line F·[Bnum]_E — the
    thm:rigidcyclo / thm:exactcount analogue characterizing when rho(S),rho(S') give equal
    slope z, and counting the characteristic-zero slope values.
(2) Index the resulting packing count by reserve eta=sigma/n and isolate the threshold
    eta*_{AA}(rho,q_gen) (arbitrary-anchor analogue of taustar) at which Lambda^{aper} drops
    from Theta(q_line) (sub-reserve; banked sigma=1) to n^{1+o(1)} (above reserve; = conj:B, open).
(3) Establish the prob:F2 ledger: above eta*_{AA}, the slope-image entropy is governed by
    q_gen=p and the quotient profile Q_H(eta), not by q_line=p^2.
The two separations the restored wall names (tangent/zero-numerator, quotient-periodic) are
necessary but provably non-binding: the banked sigma=1 counterpacket survives both with
Theta(q_line) slopes. Reserve is the missing third axis.
```

Field and status audit:
`q_gen=p=|B|`: generated field; `D⊂B` and the readout components `interp_S(w0),interp_S(w1)` lie in `B[X]`, so the readout is a `q_gen`-entropy object. PROVED (Cycle 1–2 reduction, re-derived in Step 1). `q_line=p^2=|F|`: the slopes `z` and `E` live here; the slope-image cardinality is a `q_line` quantity, and sub-reserve it equals `Theta(q_line)` (banked counterpacket, Step 3). `q_chal`: not in play — no protocol theorem is claimed, and nothing here licenses dividing an MCA numerator by `q_chal=|F|`. `B`,`F`: kept separate throughout; `Ehat=lcm(E,E^tau) in B[X]` is the only base/extension bridge used. Proved: the identification wall-count `= Lambda^{NC}` for the datum (balanced, `Bnum!=0`), via banked Cycle 1/2/3 facts; and that both named separations leave a `Theta(q_line)` survivor at `sigma=1`. Not proved (open, correctly): any nontrivial packing bound above corrected reserve (`= conj:B`/`prob:perfiber` in arbitrary-anchor extension-denominator form); the rigidity law for `rho`; the `prob:F2` ledger. Not claimed: `ass:extension-mca-lift`, protocol denominator saving, list/line decoding.

What to bank:
Bank that restored `W-F1-AA` is a faithful arbitrary-anchor extension-denominator instance of `Lambda^{NC}`/`prob:perfiber`, not a smaller object, and that its two named separations (tangent/zero-numerator, quotient-periodic) are non-binding: the banked F1 Fixed-Rate Sigma-1 Counterpacket survives both and yields `Theta(q_line)` slopes, so "balanced `t=sigma`" carries a hidden reserve `eta=sigma/n` that is the actual binding constraint. Promote the live target from "bound the paired-readout slope image" to `W-F1-AA-RES`: supply (1) a rigidity/value-count law for `rho` mod `Ehat` (the `thm:rigidcyclo`/`thm:exactcount` analogue, currently existing only for the monic prefix), (2) the reserve threshold `eta*_{AA}(rho,q_gen)` separating the `Theta(q_line)` sub-reserve regime from the conjectural `n^{1+o(1)}` above-reserve regime, and (3) the `prob:F2` generated-field ledger for the above-threshold image. Keep the Cycle 3 noncontainment lemma and Cycle 2 slope-uniqueness caveat; do not re-bank the `sigma=1` counterpacket as a refutation of the corrected-reserve wall.