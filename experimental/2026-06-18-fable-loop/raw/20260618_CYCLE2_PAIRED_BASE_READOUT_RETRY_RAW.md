I'll start by reading the file index, then the specific files needed for this audit.Now let me read the key TeX definitions. Based on the raw notes, `def:residue` is around line 1189 in slackMCA_v3.tex.Now let me read the relevant parts of snarks_v4.tex (ledger discipline around `ass:extension-mca-lift`, `op:extension-mca`, `op:line-decoding`) and `prob:F1` in proximity_blueprint_v3.tex.Now let me find `op:extension-mca` and `op:line-decoding` in snarks_v4.tex, and `prob:F1` in proximity_blueprint_v3.tex.Let me find `op:extension-mca` and `op:line-decoding` in snarks_v4.tex.Let me locate the `op:extension-mca` and `op:line-decoding` labels directly.The bash tool isn't available. Let me locate the open-problem labels by reading the relevant section of snarks_v4.tex.Let me find the exact open-problem statements `op:extension-mca` and `op:line-decoding`.Let me find the open-problems section with `op:extension-mca` and `op:line-decoding`.The open-problem labels must be in a dedicated section. Let me read around lines 745-800.Now let me read the state files and the directly relevant supporting audits/verifiers.I have all the source I need: `def:residue`/`lem:denom`/`thm:normalform`/`prob:perfiber` (slackMCA_v3.tex), the ledger discipline around `ass:extension-mca-lift`/`op:extension-mca`/`op:line-decoding` (snarks_v4.tex), `prob:F1` (proximity_blueprint_v3.tex), the Cycle 1 audit/raw, and the L1 overcount verifier. The factorization checks out under scrutiny; the operative finding is that the raw fiber endpoint is false.

```text
Final classification: EXACT_NEW_WALL
```

Statement:

The factor-through-readout reduction is TRUE and minimal, but it does not deliver a packing bound; the correct next target is a slope-image / bad-locus packing problem, and the raw uniform per-fiber endpoint is provably false.

Reduction (true). Let `B=F_p`, `F=F_{p^2}=B[α]`, `τ:x↦x^p`, `D=H⊆B`, `k=ρn`, balanced `a=k+t`, `t=σ`, radius fixed so `s_δ=a`. Fix a `def:residue` datum `(E,Bnum,w)`, `deg E=t`, `E` nonzero on `D`, `deg Bnum<t`, `Bnum≠0`, `w:D→F` arbitrary. Put `Ehat=lcm(E,E^τ)∈B[X]`. Then for every slope `z` with a witness, and every `a`-subset `S` of its support, the slope condition `[interp_S(w)]_E=z[Bnum]_E` in `R:=F[X]/(E)` is the value of a fixed `F`-rational map `Ψ` applied to the paired base readout
`ρ(S)=(interp_S(w0) mod Ehat, interp_S(w1) mod Ehat)∈(B[X]/Ehat)^2`, where `w=w0+αw1`, `w0,w1:D→B`. So `#slopes` is at most the number of distinct noncontained `ρ(S)` landing on a fixed `F`-line. No extension-only `S`-dependent invariant survives; `F` enters only through the fixed scalar `α` and the fixed map `Ψ`.

The wall (sharpened endpoint, `W-F1-AA`). Prove
`Λ^{NC}_{σ,δ}(H,k)=#{ z∈F : z has a noncontained a-subset witness } ≤ n^{1+o(1)}`,
equivalently bound the number of distinct readout classes `ρ(S)`, `S∈binom(H,a)`, that simultaneously (i) lie on the fixed `F`-line `F·[Bnum]_E ⊆ R` (the bad locus; `t−1` fixed `F`-linear conditions after reducing `ρ(S)` mod `E`) and (ii) are noncontained (admit no global degree-`<k` explanation of both `w/E` and `−Bnum/E` on `S`). The bound must be on the slope image / distinct bad-locus classes, never on the fibers of `S↦ρ(S)`. For the monic stratum `w0=X^a|_H`, `w1=0`, `ρ(S)` collapses to the single locator class `[L_S]_{Ehat}` and the statement is exactly `prob:perfiber` (prefix/symmetric-function collisions); the wall is its extension from symmetric functions to arbitrary base interpolation residues at modulus degree `≤2t`.

Proof or disproof:

Reduction, three steps (all source-valid).

(A) Forced interpolant. `def:residue` gives `deg Q_z<k+t=a` and `|S_z|≥s_δ=a`. On any `a`-subset `S⊆S_z`, `Q_z` agrees with `w` at `a` points and has degree `<a`, so `Q_z=interp_S(w|_S)` uniquely (Vandermonde). Supports of size `>a` only over-determine `Q_z`, so every slope is captured by some `a`-subset. This is why `t=σ` is rigid: residual slack `σ−t=0`, so the `RESIDUAL_SLACK` list reduction yields nothing and the interpolant is forced.

(B) Base split, no extension freedom. `S⊆D⊆B`, so each Lagrange basis polynomial `ℓ_{S,s}=∏_{s'≠s}(X−s')/(s−s')∈B[X]`. Interpolation is `F`-linear in the sampled values, hence `interp_S(w)=interp_S(w0)+α·interp_S(w1)` with both components in `B[X]`, degree `<a`. The only `S`-dependent data are the two base polynomials; `α` is a fixed constant.

(C) Factor through `Ehat`. `Ehat` is `τ`-fixed (`Ehat^τ=lcm(E^τ,E)=Ehat`), so `Ehat∈B[X]` and `interp_S(w_i) mod Ehat∈B[X]/(Ehat)`. Since `E∣Ehat`, reducing `ρ(S)` further mod `E` recovers `[interp_S(w)]_E=[interp_S(w0)]_E+α[interp_S(w1)]_E`, hence the slope condition. The map `Ψ` (reduce mod `E`, combine with fixed `α`, test membership of the fixed `F`-line `F·[Bnum]_E`, output `z`) depends only on `(α,E,Bnum)`, not on `S` or `w`. So `slope-condition = Ψ∘ρ`. ∎

Disproof of the raw uniform fiber endpoint. The statement "for every `(w0,w1)` every fiber of `S↦ρ(S)` has size `≤n^{1+o(1)}`" is false. Take `w0=P|_H` with `deg P<a` (a codeword anchor) and `w1=0`. Then `interp_S(w0)=P` for every `a`-subset `S`, so `ρ(S)=(P mod Ehat,0)` is constant: the fiber is all `binom(n,a)` subsets, superpolynomial. This is the exact mechanism of `verify_l1_arbitrary_fiber_overcount.py` (a degree-`<k` `U` makes every `s`-subset feasible while the list is 1). But a huge fiber here corresponds to one readout class and one slope, and that slope is the contained/tangent case removed by noncontainment. So fiber size is unbounded while the slope count is controlled; the endpoint must be a slope-image / distinct-bad-locus-class bound, not a fiber bound. This refutes the raw `prob:perfiber`-style generalization quantified over all `w0,w1`, and is why the classification is `EXACT_NEW_WALL` rather than a clean `BANKABLE_LEMMA` endpoint.

Not a `COUNTERPACKET`. The micro-witness (two supports `S≠S'` with `Ehat∣(L_S−L_{S'})` but distinct interpolation residues under a perturbed base anchor) shows only that the interpolation-residue readout strictly refines the locator readout — it separates supports the locator merges. Separating two supports into two slopes is not superpolynomially many noncontained slopes, so no counterpacket is exhibited; the separation lives entirely inside `B`.

Mandatory checks:

1. Ehat for arbitrary/mixed denominators. Yes, and it is minimal. For any `E` (mixed `E=G·E_1`, `G=gcd(E,E^τ)∈B[X]`, `gcd(E_1,E_1^τ)=1`), `E∣Ehat`, so `ρ(S)` determines `interp_S(w) mod E`. Conversely any `M∈B[X]` with `E∣M` satisfies `M=M^τ`, hence `E^τ∣M`, hence `Ehat∣M`: `Ehat=lcm(E,E^τ)` is the smallest base modulus that works. `deg Ehat=deg G+2deg E_1≤2t`, with `Ehat=E` (no doubling) when `E∈B[X]` and `deg Ehat=2t` when `E` is coprime to `E^τ`. A single datum carries one `E`, so no simultaneously-mixed-denominator case arises within a datum.

2. Uniqueness of `z` when `[Bnum]_E` is a nonzero zero-divisor in `R`. Yes, `z` is unique whenever it exists. Uniqueness only needs the scalar map `F→R`, `z↦z[Bnum]_E`, to be injective. If `c[Bnum]_E=0` for a scalar `c∈F`, then `E∣c·Bnum`; `c≠0` is a unit in `F`, so `E∣Bnum`, contradicting `0≠deg Bnum<deg E`. Hence no nonzero scalar annihilates `[Bnum]_E`, regardless of whether `[Bnum]_E` is a zero-divisor in the full algebra `R`. Existence (landing on the `F`-line) can fail; uniqueness cannot.

3. Replacing `|S_z|>a` by an `a`-subset. It preserves every slope (no slope lost: `Q_z` is the unique interpolant on each `a`-subset, all consistent) but produces an upper bound with overcounting, not an exact count. Overcount has two sources: many `a`-subsets give the same slope (many-to-one), and some `a`-subsets satisfy the readout without extending to a size-`≥s_δ` noncontained witness (spurious). So the move is sound for `#slopes≤#bad a-subset readouts`; the overcount is exactly what forbids reading a fiber bound as a slope bound (check 5).

4. Correct dimension ledger. The readout target `(B[X]/Ehat)^2` has `F_p`-dimension `e·deg(Ehat)=2·deg(Ehat)≤4t` (here `e=[F:B]=2`); not `deg(Ehat)`, which is the single-copy monic-locator ledger `≤2t`. The factor `e` is the `w0,w1` doubling, not an extension-valued blowup. Caveat: this is the ambient readout dimension; the active packing object is smaller, cut by the `t−1` `F`-linear bad-locus conditions and the noncontainment quotient, so the effective value is `≤e·deg(Ehat)` and the bound to prove is `n^{1+o(1)}`, not `q_gen^{deg}`.

5. Why the raw uniform fiber endpoint is false / how to repair. False: codeword/low-degree anchors `w0=P|_H`, `deg P<a`, make `interp_S(w0)=P` for all `S`, so `ρ(S)` is constant and the fiber is all `binom(n,a)` subsets (the `verify_l1_arbitrary_fiber_overcount.py` phenomenon). Repair: bound the slope image, i.e. the number of distinct `ρ(S)` that land on the fixed `F`-line `F·[Bnum]_E` and are noncontained. The codeword anchor then contributes one class and is removed as contained/tangent, so the superpolynomial fiber does not become superpolynomial slopes. The monic special case of this repaired statement is `prob:perfiber`.

What to bank:

Bank the reduction as a true, minimal `BANKABLE_LEMMA` (Theorems A–C: balanced `t=σ` arbitrary anchors over `F=F_{p^2}` with `D⊆B` split as `w=w0+αw1` and the `def:residue` slope condition factors as `Ψ∘ρ` through `ρ(S)=(interp_S(w0) mod Ehat, interp_S(w1) mod Ehat)∈(B[X]/Ehat)^2`, `Ehat=lcm(E,E^τ)` the minimal base modulus, `F_p`-dimension `2 deg Ehat≤4t`, `z` unique when it exists). It implies there is no extension-only arbitrary-anchor invariant above the monic case, so the `q_gen=p` entropy reserve (`eq:entropy-necessary`), not `q_line=p^2`, still governs. It does not imply a packing bound, does not prove `ass:extension-mca-lift`/`op:extension-mca`, does not touch the list ledger `eq:extension-list` (unbalanced import only) or `op:line-decoding`, and must not be read as a raw per-fiber bound. The live object is `EXACT_NEW_WALL W-F1-AA`: bound the distinct noncontained readout classes on the fixed `F`-line, the arbitrary-base-anchor generalization of `prob:perfiber` at modulus degree `≤2t`; raw uniform fiber bounds over all `(w0,w1)` are refuted by codeword anchors.